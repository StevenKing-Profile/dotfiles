[user]
    name = Your Name
    # Example: Inject different emails based on the machine's OS or Hostname
    {{ if eq .chezmoi.os "darwin" }}
    email = mac.user@domain.com
    {{ else }}
    email = linux.user@domain.com
    {{ end }}

[credential]
    # macOS uses the native keychain. Linux uses memory cache (1 hour timeout).
    {{ if eq .chezmoi.os "darwin" }}
    helper = osxkeychain
    {{ else if eq .chezmoi.os "linux" }}
    helper = cache --timeout=3600
    {{ end }}

[core]
    # Use the Rust-based pager you just installed
    pager = delta
    editor = vim

[init]
    # Modern default branch naming
    defaultBranch = main

[pull]
    # Rebase by default to prevent messy merge commits when pulling
    rebase = true

[push]
    # Only push the current branch to its corresponding remote branch
    default = current
    # Automatically setup remote tracking if it doesn't exist
    autoSetupRemote = true

[fetch]
    # Automatically prune remote-tracking branches that were deleted on the server
    prune = true

[alias]
    st = status -sb
    co = checkout
    br = branch
    ci = commit
    # Force push safely: verifies no one else has pushed to the branch before overwriting
    pf = push --force-with-lease
    # A clean, one-line graphical log for terminal viewing
    lg = log --graph --abbrev-commit --decorate --format=format:'%C(bold blue)%h%C(reset) - %C(bold green)(%ar)%C(reset) %C(white)%s%C(reset) %C(dim white)- %an%C(reset)%C(auto)%d%C(reset)' --all

[interactive]
    diffFilter = delta --color-only

[delta]
    navigate = true
    light = false
    line-numbers = true
    side-by-side = true

[merge]
    conflictstyle = diff3

[diff]
    colorMoved = default
